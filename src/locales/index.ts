import { pt } from './pt';
import { en } from './en';

const locales = {
  pt,
  en,
};

export const languages = [
  { name: 'pt', title: 'Português' },
  { name: 'en', title: 'English' },
];

export { locales };
